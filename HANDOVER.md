# ResQ · תדריך מצב לקריאה חיצונית

מסמך מסירה. נכתב כדי שמודל או מפתח שלא ראה את הפרויקט יוכל
להיכנס אליו בקריאה אחת ולומר משהו מועיל.

עדכון אחרון: 23 באוגוסט 2026 — כולל שלב 8 (אבטחות) ותיקוני
האבטחה שנבעו מהביקורת החיצונית.

---

## 1. מה זו המערכת

**ResQ** — מערכת ניהול כוח אדם לתחנות כיבוי אש בישראל.
נבנית לתחנה 102 באילת, בתכנון להרחבה לתחנות נוספות.

היא מחליפה מערכת קיימת שכתובה ב-Google Apps Script מעל Google
Sheets, ושמשמשת היום את כבאי אילת בפועל. המערכת הישנה עובדת
ואינה נוגעים בה. חלק מהחוקיות העסקית הועתקה ממנה במכוון,
אחרי קריאה בקוד שלה — וחלק **לא** הועתק במכוון, כי היו שם
באגים (מפורט בסעיף 5).

בעל המוצר: כבאי בתחנה, לא מפתח. הוא מגדיר את הכללים העסקיים
ומריץ את הפריסות. כל שינוי בקוד נכתב לתיקייה שלו במחשב.

---

## 2. סטאק ואילוצים

| רכיב | מה |
|---|---|
| בסיס נתונים | Firestore (Native, `eur3`) |
| שרת | Cloud Functions v2, Node 22, `europe-west1` |
| הזדהות | Firebase Auth + **Custom Claims** |
| התראות | Firebase Cloud Messaging (Web Push) |
| אירוח | GitHub Pages — `elyo102.github.io/ResQ-102/` |
| חזית | HTML+ES modules סטטיים. **אין build, אין framework, אין npm בצד הלקוח** |
| מזהה פרויקט | `station-102` (לא השתנה כששם המוצר שונה) |

### אילוצים שחשוב להכיר לפני שמציעים משהו

1. **אין שלב בנייה בחזית.** כל מסך הוא קובץ HTML יחיד שמייבא
   מודולים מקומיים ואת Firebase מ-gstatic. הצעה שמערבת React,
   Vite, TypeScript או bundler היא הצעה לכתוב את הפרויקט מחדש.
   פסילת מטמון נעשית ידנית בשאילתת `?v=N` על כל ייבוא מקומי
   (כרגע `?v=19`).

2. **כללי האבטחה אינם מבצעים `get()`.** אף כלל לא קורא מסמך
   אחר. כל ההרשאות נקראות מה-ID token דרך
   `request.auth.token.get(key, '')`. זו החלטה שהניעה את כל
   מודל ההרשאות — היא חוסכת קריאה בכל בדיקה ומונעת שרשראות
   כללים. אילוץ זה מחייב שכל החלטת הרשאה תהיה נגזרת מה-claims
   בלבד.

3. **אין גישת push מסביבת הפיתוח.** הקוד נכתב ישירות לתיקייה
   במחשב של בעל המוצר, והוא מריץ `web-2-upload.bat` שדוחף
   ל-GitHub. אין CI.

4. **כללי האבטחה מעולם לא הודרו מקומית.** הורדת ה-emulator
   חסומה ברשת בסביבת הפיתוח. הוודאות היחידה על תקינות
   התחביר היא הפלט של `deploy-rules.bat`. **זו נקודת התורפה
   הגדולה ביותר בפרויקט.**

5. **RTL.** כל הממשק בעברית. טווחי שעות דורשים
   `direction:ltr; unicode-bidi:isolate`, אחרת `07:00–08:00`
   מוצג כ-`08:00–07:00`. `input[type=time]` דורש
   `color-scheme:dark`, אחרת אייקון השעון שחור על שחור.

---

## 3. מה בנוי

13,600 שורות בקירוב. 13 מסכים, 10 מודולים, 22 פונקציות ענן,
784 שורות כללי אבטחה.

### מסכים

| קובץ | שורות | מה |
|---|---|---|
| `login.html` | 966 | כניסה במספר עובד, פרופיל, מסך בית |
| `attendance.html` | 1225 | דיווח נוכחות ושעות, דוח חודשי |
| `board.html` | 958 | לוח ציוות וכשירות מבצעית |
| `admin.html` | 860 | ניהול משתמשים, סבבים, הרצות ידניות |
| `alerts.html` | 849 | התראות פוש, הודעות, קריאת פתע |
| `quals.html` | 797 | כשירויות וקו אדום |
| `schedule.html` | 768 | סידור עבודה, לוח שנה אישי |
| `guards.html` | 829 | יומן אבטחות, הרשמה, שיבוץ, חלוקת עומס |
| `swaps.html` | 730 | החלפות משמרת |
| `access.html` | 389 | אישור נרשמים, הרשאות |
| `check.html` | 342 | מסך אבחון |
| `unlock.html` | 89 | שחרור חשבון נעול |

### מודולים משותפים

| קובץ | מה |
|---|---|
| `hours.js` | חישוב שעות. סוגי יום, משמרת מפוצלת, שעות נוספות, חובת נימוק |
| `rotation.js` | סבב משמרות, חריגות, השפעת החלפה מאושרת על הסידור |
| `readiness.js` | חישוב כשירות אחד המשרת שלושה מסכים |
| `guards.js` | אבטחות — חישוב עומס הוגן, המלצת שיבוץ, זמינות |
| `callout.js` | קריאת פתע — החלון הקופץ והתשובה |
| `push.js` | רישום מכשיר להתראות, סוגי התראה |
| `nav.js` | סרגל ניווט, הצגה לפי תפקיד |
| `stations.js` | שמות תחנות ומרחבים |
| `firebase-config.js` | הגדרות |
| `firebase-messaging-sw.js` | Service Worker להתראות ברקע |

### פונקציות ענן (22)

**הזדהות והרשאות**
`bootstrapSuperAdmin` · `approveRegistration` · `setUserRole` ·
`loginWithEmployeeNumber` · `requestPasswordReset` ·
`unlockAccount` · `listUsersWithClaims` · `whoAmI` ·
`reindexDirectory`

**מתוזמנות**
`nightlyScan` (02:10 — איתור חריגות ופערים) ·
`monthlyHrReport` (1 בחודש, 06:00 — דוח שעות למשאבי אנוש) ·
`hoursReminder` (17:00, נורה רק כשנשארו 4 ימים לסוף החודש) ·
`guardReminder` (19:00, למי ששובץ לאבטחה מחר) ·
`runReportNow` (הרצה ידנית)

**התראות**
`onSwapChange` · `onReportChange` · `sendBroadcast` ·
`sendCallout` · `closeCallout` · `onGuardOpen`

**אבטחות**
`guardSignup` · `assignGuard`

### אוספי Firestore

תחת `stations/{sid}/`:
`users` · `roster` · `directory` · `emp_index` · `presence` ·
`quals` · `member_quals` · `config/{redline|board|hr|push}` ·
`shifts/{crew}` · `rotations` · `shift_overrides` ·
`sub_stations` · `vehicles` · `attendance` · `monthly_reports` ·
`scans` · `hr_reports` · `swaps` · `push_tokens` · `broadcasts` ·
`callouts` · `guards` · `pending_users` · `registration_requests` ·
`login_attempts` · `unlock_tokens` · `security_events` ·
`admin_audit` · `mail`

---

## 4. מה כל שלב עושה

### שלב 1 — הזדהות והרשאות

כניסה במספר עובד (לא במייל). Custom Claims הם מקור האמת
היחיד: `role`, `shift`, `stationId`, `districtId`, `emp`,
`super`.

תפקידים: `firefighter`, `commander`, `hr_coordinator`,
`district_commander`, ומנהל-על (`super: true`).

**כלל מנחה:** מפקד משמרת רואה ומנהל רק את המשמרת שלו. רכז
כוח אדם ומנהל-על רואים את שלוש המשמרות. הכלל הזה נאכף פעמיים
— בכללי האבטחה (`seesShift`, `seesCrewData`) ובחזית
(`visibleCrews`). שתי ההגדרות חייבות להישאר זהות; פער ביניהן
מייצר מסך שמציג כפתור שהשרת ידחה.

**החריגים לכלל, וכולם מכוונים.** הכלל חל על *נתוני* משמרת —
מי עובד, מי כשיר, מי דיווח מה. הוא אינו חל על *כלים תפעוליים*,
שבהם צמצום למשמרת היה שובר את הכלי עצמו:

| אוסף | למה כלל-תחנתי |
|---|---|
| `broadcasts` | הודעה לכל התחנה היא בדיוק מה שהכלי עושה |
| `callouts` | הזעקה חוצה משמרות בהגדרה — היא מוציאה אנשים מיום החופש שלהם, ויום חופש הוא בהגדרה יום שהמשמרת שלך לא עובדת בו |
| `guards` | אבטחה ביום חופש מגייסת מכל התחנה. ההרשמה פתוחה לכולם |
| `roster` | מאגר השמות לשיבוץ. בלוח הציוות אלדד ביקש במפורש שיבוץ כלל-תחנתי |
| `presence` | קובץ בקרת גישה. סגל בלבד |

**מה כן חייב להיות מצומצם למשמרת** — וכל חריגה כאן היא באג:
`attendance`, `monthly_reports`, `shifts`, `member_quals`,
`scans`, `hr_reports`. אלה נתוני אנשים, לא כלים.

**נקודה עדינה:** `seesShift` מחזיר אמת גם כאשר `myShift()`
ריק. מפקד שטרם שויך למשמרת רואה את כולן. זו החלטה מכוונת —
אחרת הוא חסום מהכל בלי שום דרך להבין למה — אבל היא חריג ישיר
לכלל, וכל בדיקה שלו צריכה להתייחס אליה.

### שלב 2 — כשירויות וקו אדום

כשירויות מוגדרות לכל תחנה בנפרד ונערכות בידי מפקד משמרת,
רכז כוח אדם או מנהל-על. אין תפוגה.

**קו אדום** = תקן מינימלי למשמרת: מספר אנשים + הרכב כשירויות
(למשל: לפחות נהג אחד, לפחות שני חובשים). ירידה מתחתיו
**מסומנת באדום ואינה חוסמת** — זו הייתה החלטה מפורשת של בעל
המוצר. מערכת שחוסמת ציוות בשעה שלוש בלילה היא מערכת שעוקפים.

### שלב 3 — ציוות וכשירות מבצעית

לוח רחב: רכבים (אלמוג, געש, סער — שמות אמיתיים), ובכל רכב
תפקידים ושמות. ראש המשמרת מוסיף רכבים, משנה שמות תפקידים,
ומשבץ אנשים.

**השיבוץ פתוח לכלל התחנה, לא רק למשמרת.** אדם ממשמרת אחרת
שיכול לעבוד — משובץ. יש שורת חיפוש שם להוספה מהירה.

חריגות סבב (`shift_overrides`) בארבעה סוגים: החלפת משמרת,
חג או מועד, אימון או הדרכה, כוננות מיוחדת.

תחנות משנה: אילת היא התחנה הראשית (שם המטה ומפקד התחנה),
ותחתיה שחמון, תמנע, יטבתה. אדם משויך לתחנה אחת ביום.

### שלב 4 — נוכחות ושעות

זה הלב של המערכת, וזה החלק שהועתק מהמערכת החיה בקפידה.

**סוגי יום**

| סוג | שעות | דורש שעות כניסה/יציאה | דורש נימוק |
|---|---|---|---|
| רגיל | מחושב | כן | לא |
| החלפה לצרכי מערכת | מחושב | כן | **כן** |
| שעות ידני · נע״ת | מחושב | כן | **כן** |
| ישיבות | מחושב | כן | **כן** |
| חופש | 24 | לא | לא |
| מחלה | 0 | לא | לא |
| מילואים | 8.5 קבוע | לא | לא |

**צורת משמרת** — שדה מפורש, לא ניחוש:

- `regular` — משמרת רגילה
- `continued` — המשך משמרת, מעל 24 שעות. מחייב `> 24`, אחרת
  השמירה נדחית
- `split` — משמרת מפוצלת: כניסה, יציאה, כניסה, יציאה באותו
  היום. סך השעות הוא סכום שני הקטעים

**תחנות משנה עם שעות קבועות** — יטבתה = 25 שעות קבועות.
המספר הקבוע גובר על החישוב וגם על הפיצול, ו-25 שעות ביטבתה
**אינן חריגה ואינן דורשות נימוק**. (טעות זו נעשתה ותוקנה
פעמיים במהלך הפיתוח.)

**שעות נוספות** = מעל אורך המשמרת בסבב. נימוק חסר **חוסם
שמירה** — כאן כן חוסמים, בניגוד לקו האדום, כי דוח בלי נימוק
מגיע לשכר ונדחה שם ממילא.

**אישור חודשי** — מפקד משמרת ורכז כוח אדם מאשרים. מאשר רשאי
לפתוח דוח מחדש. אפשר לחזור חודש אחורה. יש כפתור "חשב מחדש"
לפערים.

**סריקה לילית** (02:10) — מאתרת פערים בין הדיווח לסידור
וחריגות שעות. חריגה = מעל 265 שעות בחודש.

**דוח חודשי למשאבי אנוש** — נשלח אוטומטית ב-1 לחודש ב-06:00,
במייל אחד מאוחד לכל המשתמשים, אל `lisaa@102.gov.il`
(ליסה עגיב). כולל פילוח לפי תחנות משנה. **אין כפתור הדפסה** —
זו הייתה דרישה מפורשת: הדוח נשלח מהמערכת, לא מודפס.

### שלב 5 — החלפות משמרת

יום תמורת יום. מסלול חמישה מצבים:

```
peer      → נשלחה, הכבאי השני צריך להסכים
cmd_from  → הצד השני הסכים, מפקד המבקש בוחן
cmd_to    → מפקד המבקש אישר, מפקד המשמרת השנייה בוחן
approved  → שני המפקדים אישרו
rejected  → נדחתה, עם נימוק ועם שם הדוחה
```

כשמפקד בוחן בקשה, מוצגות לו **הכשירויות של שני הכבאים זו מול
זו**. פער בכשירויות **מזהיר ואינו חוסם**.

שני הכבאים מקבלים התראה עם ניסוח כמו: "אושר ע״י רמי חנן
(ראש משמרת א׳)" — כולל שם ותפקיד המאשר, גם באישור וגם בדחייה.

**החלפה מאושרת משנה את הסידור בפועל.** `swapEffect` ו-
`personWorks` ב-`rotation.js` מוזרקים לתוך הסידור ולתוך
מילוי הנוכחות מראש. (זה היה באג: כל מסלול האישור נבנה, ואז
הסידור התעלם ממנו לגמרי.)

### שלב 6 — התראות פוש

**אוטומטיות:** בקשת החלפה חדשה → למפקד שצריך לאשר. החלפה
אושרה/נדחתה → לשני הכבאים. דוח הוגש → למפקד ולרכז. דוח
אושר/נפתח מחדש → לכבאי. ממצא מהסריקה → לכבאי.

**מתוזמנת:** תזכורת דיווח שעות, 17:00, כשנשארו בדיוק 4 ימים
לסוף החודש. התנאי הזה הועתק מילה במילה מ-`dailyReminderCheck_`
במערכת הישנה.

**ידנית (`sendBroadcast`):**

| שולח | יכול לשלוח אל |
|---|---|
| מפקד משמרת | המשמרת שלו |
| כבאי | המשמרת שלו |
| רכז כוח אדם | כל התחנה, או משמרת נבחרת |
| מנהל-על | כל התחנה, או משמרת נבחרת |

נאכף בשרת, לא רק מוסתר בממשק.

**סוגי התראה:** 9. שלושה חובה (`callout`, `swap_mine`,
`report_mine`), שישה ניתנים לכיבוי.

### שלב 8 — אבטחות ואירועים

אבטחה היא הצבת כוח באירוע — משחק, הופעה, עבודות חמות, כינוס.
**באפליקציה הישנה אין לזה זכר**; השלב הזה נבנה מאפס.

**ההבחנה שהכל נשען עליה, מפי אלדד:** אבטחה שנופלת ביום שהכבאי
ממילא במשמרת נבלעת ב-24 השעות שלו — אין שעות נוספות ואין שכר
נוסף. אבטחה ביום החופש שלו היא יציאה מהבית.

לכן המערכת סופרת אותן **בנפרד**, ומדד ההוגנות מסתכל בעיקר על
אלה שביום חופש. אדם שעשה שמונה אבטחות בתוך המשמרות שלו אינו
עמוס יותר ממי שעשה חמש בימי החופש שלו. זו הטעות המתבקשת כאן,
והיא כתובה במפורש בראש `guards.js`.

**הרשמה עצמית.** כבאי לוחץ "אני מעוניין" — הבעת עניין, לא מקום
שמור. **אין מצב "מלאה"**: מספר הנרשמים אינו סוגר את ההרשמה,
אחרת מי שזריז תופס הכל, וזו בדיוק הבעיה שהמסך בא לפתור.

**השיבוץ.** המפקד רואה את הנרשמים כשהמומלצים כבר מסומנים,
ולוחץ שמור. הדירוג: הכי פחות אבטחות ביום חופש ← מי שלא יצא
הכי הרבה זמן ← אבטחות בתוך משמרת ← שם. **הסדר יציב בכוונה** —
מפקד שרואה רשימה שמשתנה בכל כניסה מפסיק לסמוך עליה.

חסימות: מי שבחופש/מחלה/מילואים, ומי שמשובץ לאבטחה חופפת באותו
יום. חסרי כשירות יורדים בדירוג אך אינם חסומים.

**אוטומטי ביומן.** אבטחה נשמרת עם תאריך בלבד ולכן נופלת לסידור
מעצמה — נקודה ירוקה ביום. אין לוח נפרד לתחזק.

**בדוח שעות.** סוג יום `guard`. שיבוץ ביום חופש ממלא מראש את
השעות ואת שם האירוע. אבטחה בתוך המשמרת אינה נרשמת בנפרד.

**נעילה אחרי שיבוץ:** אבטחה שיש עליה משובצים אינה ניתנת
לעריכה מהדפדפן. מי שכבר תכנן את היום שלו לא אמור לגלות שהתאריך
זז.

### תיקוני אבטחה, 23.8.2026

ביקורת חיצונית על `firestore.rules` מצאה חמישה ליקויים. כולם
אומתו מול הקוד ותוקנו:

1. **קריאת פתע** — כל חבר תחנה יכול היה לענות לקריאה שלא
   נשלחה אליו ולנפח את ספירת המגיעים. נוסף
   `request.auth.uid in resource.data.get('uids', [])`.
2. **קריאת פתע** — `.diff()` מזהה שינוי בתוכן הערך אך אינו
   מאמת אותו, ולכן אפשר היה לכתוב **שם של אדם אחר** תחת
   התשובה שלך. נוסף אימות ל-`resp`, והמסך קורא כעת את השם
   מהסגל לפי ה-uid ולא מתוך התשובה.
3. **החלפות** — `from_crew`/`to_crew` לא היו נעולים בעדכון,
   ולכן צד לבקשה יכול היה לשנות מי המפקד המורשה לאשר אותה.
   נעולים כעת בכל ארבעת כללי העדכון.
4. **החלפות** — אדם אחד יכול היה להעביר בקשה בשני שלבי
   המפקדים לבדו (רכז כוח אדם ומנהל-על עוברים `seesShift` לכל
   המשמרות). נוסף
   `request.auth.uid != resource.data.get('from_appr_uid', '')`.
5. **`scans` / `hr_reports`** — מסמך אחד לחודש עם השעות של כל
   התחנה, פתוח לכל הסגל. צומצם לרכז כוח אדם ולמנהל-על. אף מסך
   לא קורא אותם, ולכן העלות אפס.

**פתוח להחלטה:** האם מפקד רשאי לאשר החלפה שהוא צד בה, והאם
`from_emp`/`to_emp` צריכים להיעלם מתצוגת ההחלפות (מספר עובד
אינו חשוף בשום אוסף אחר לעמיתים).

### שלב 7 — קריאת פתע

הזעקה. הכלי היחיד במערכת שאי אפשר לכבות.

**מי שולח:** ראש משמרת, רכז כוח אדם, מנהל-על. כבאי — לא.

**אל מי:**
- המשמרת שלי — לחיצה אחת
- אנשים נבחרים בשמם — חיפוש, **פתוח לכל התחנה** ולא רק
  למשמרת השולח
- משמרת אחרת שלמה, או כל התחנה — רכז כוח אדם ומנהל-על בלבד

ההיגיון: אירוע שמצריך הזעקה לא עוצר בגבול המשמרת, אבל מפקד
שצריך אנשים ממשמרת אחרת בוחר אותם בשמם — ולא לוחץ כפתור אחד
שמעיר תשעים איש. שם המזעיק נשמר על כל קריאה; זו הבקרה.

**מה הכבאי רואה:** חלון אדום מלא מסך, בכל מסך שהוא נמצא בו.
אין X ואין סגירה בלחיצה על הרקע. שתי אפשרויות בלבד: **מגיע**
או **לא זמין**. עד שלא ענה — החלון חוזר.

**איך זה עוקף כיבוי:** הסוג `callout` נמצא ברשימת ה-`must`
בתוך `pushToUsers` בשרת. העדפות המשתמש לא נבדקות עבורו.

**מה המפקד מקבל בחזרה:** כרטיס "קריאות פתוחות" עם ספירה חיה —
מגיעים / לא זמינים / לא ענו — ורשימת שמות. `onSnapshot`, ללא
רענון. כפתור "סגור קריאה" מפסיק את ההקפצה.

**מודל האחסון:** התשובות נשמרות כמפה `acks` **על מסמך הקריאה
עצמו**, לא באוסף משנה — כדי שהמפקד יראה הכול בקריאה אחת בזמן
אמת. כלל האבטחה מרשה למשתמש לגעת אך ורק במפתח ששמו עליו:

```
allow update: if member(sid)
  && request.resource.data.diff(resource.data)
       .affectedKeys().hasOnly(['acks'])
  && request.resource.data.get('acks', {})
       .diff(resource.data.get('acks', {}))
       .affectedKeys().hasOnly([request.auth.uid]);
```

יצירה ומחיקה מהדפדפן **חסומות לחלוטין** (`allow create,
delete: if false`) — קריאת פתע נוצרת רק בפונקציית השרת.

---

## 5. באגים שלא הועתקו מהמערכת הישנה

שלושה, במכוון:

1. **סוג היום היה מוסק ממחרוזת חופשית.** כאן הוא שדה אמיתי
   עם ערכים סגורים.
2. **יטבתה זוהתה בהתאמת מחרוזת על שם המקום.** כאן היא רשומה
   באוסף `sub_stations` עם `fixed_hours: 25`.
3. **סכומי שעות נשמרו במטמון והספיקו להתיישן.** כאן הם
   מחושבים בכל תצוגה.

בנוסף — הבדל מכוון אחד בחוקיות: **חופש = 24 שעות**. במערכת
הישנה חופש היה 0. בעל המוצר תיקן את זה במפורש.

---

## 6. באגים שהתגלו בפיתוח ותוקנו

רשימה חלקית, מובאת כי היא מלמדת על אופי המערכת:

| מה קרה | הסיבה | התיקון |
|---|---|---|
| הפריסה רצה מהתיקייה הלא נכונה בשקט | `cd /d` נכשל והסקריפט המשיך | שערי STOP על `firebase.json` ועל `functions/package.json`, נתיבים מוחלטים, שורת `[RESULT]` |
| מסך הכניסה הבזיק בכל מעבר בתפריט | `#authView` היה התצוגה שגלויה כברירת מחדל | `bootView` + `authSettled` עם נפילה אחורה של 6 שניות |
| "הוסף כשירות" הוצג לכל כבאי | `.addrow{display:flex}` הוכרז אחרי `.hide{display:none}` באותה ספציפיות | `.hide{display:none !important}` |
| רכז כוח אדם ננעל למשמרת שלו | פעמיים, בשני מסכים שונים | `visibleCrews` ודגל `UNLOCKED` התואם ל-`seesShift` |
| `07:00 → 09:00` — שעתיים או 26? | המערכת הישנה החזירה 2 בשקט | שדה `shape` מפורש + `end_day` |
| החלפה מאושרת לא שינתה כלום | מסלול האישור נבנה, הסידור התעלם | `swapEffect` / `personWorks` |
| טווחי שעות הוצגו הפוך | דו-כיווניות | `direction:ltr; unicode-bidi:isolate` |
| אייקון בורר השעה לא נראה | שחור על שחור | `color-scheme:dark` + היפוך `::-webkit-calendar-picker-indicator` |

---

## 7. איך זה נבדק

אין בדיקות יחידה קלאסיות. יש שלוש חבילות Playwright מול
Chromium headless, עם **מודולי Firebase מזויפים** (יירוט
`**/firebasejs/**`), הנוהגות לפי משתנה סביבה `SMOKE_ROLE`
בשישה תפקידים: `super`, `commander`, `hr`, `firefighter`,
`pending`, `none`.

| חבילה | מה היא מוודאת |
|---|---|
| `smoke.mjs` | כל מסך נטען בכל תפקיד. נכשל על כל שגיאת קונסולה או פס שגיאה אדום |
| `role.mjs` | מה כל תפקיד רואה: פריטי תפריט, כרטיסי ניהול, עריכת כשירויות, משמרות גלויות, כרטיס אישור החלפות, יעדי שליחה, כרטיס קריאת פתע, הבזק מסך הכניסה |
| `numbers.mjs` | כ-50 קביעות על חוקיות עסקית: שעות, צורות משמרת, שעות נוספות, טריגרים לנימוק, סיכום חודשי, סבב, חריגות, החלפות מאושרות |

בנוסף — סקריפטים לצילום מסך לבדיקה ויזואלית.

**מה שהבדיקות האלה לא מכסות:** את כללי האבטחה. הן רצות מול
Firestore מזויף. ראה סעיף 8.

---

## 8. מה פתוח

### חוסמים תפעוליים — דורשים גישה לקונסולה

1. **תוסף Trigger Email לא מותקן.** כל הדואר — איפוס סיסמה,
   הודעת נעילה, הדוח החודשי למשאבי אנוש — נכתב לאוסף `mail/`
   ואף פעם לא נשלח. **זהו החוסם התפעולי האחרון.**
2. **App Check, Delete Protection, Point-In-Time Recovery** —
   לא מופעלים.
3. פריסה של כל מה שנבנה מאז שינוי השם — טרם רצה מול נתונים
   אמיתיים.

### חוב טכני

4. **כללי האבטחה מעולם לא הודרו מקומית** (698 שורות). ראה
   אילוץ 4 בסעיף 2. **הפער המסוכן ביותר בפרויקט.**
5. גיליון Google `פיירסטור-102` נוצר ולא חובר.
6. אין CI. אין הרצת בדיקות אוטומטית לפני פריסה.

### שלבים שטרם נבנו

| שלב | מה |
|---|---|
| 9 | שכר — חישוב תוספות ומכפילים |
| 10 | אנליטיקה — מגמות, עומסים, ניצול כוח אדם |
| 11 | תקלות רכב, טפסים, פידבק |

---

## 9. איפה עזרה חיצונית באמת שווה משהו

הערכה כנה, כולל המקומות שבהם היא לא שווה.

### שווה — לפי סדר ערך יורד

**א. ביקורת על `firestore.rules`.**
זה הפער האמיתי. 698 שורות שמעולם לא עברו הידור, ושאין דרך
להדר בסביבת הפיתוח. קורא נוסף שמכיר את תחביר Firestore Rules
v2 יכול לתפוס שגיאת תחביר או פרצת הרשאה לפני פריסה, בלי
להריץ כלום. הקובץ עומד בפני עצמו וניתן להדביק אותו במלואו.

מה כדאי לבקש שייבדק ספציפית:
- האם `.diff().affectedKeys().hasOnly()` על שדה מפה מתנהג
  כמצופה בכלל `callouts` (זהו הכלל החדש והמורכב ביותר)
- האם `request.auth.token.get(key, '')` מכסה נכון את המקרה
  שבו ה-claim חסר לגמרי
- האם קיים מסלול שבו מפקד משמרת מגיע לנתוני משמרת אחרת
- ארבעת כללי ה-`allow update` הנפרדים על `swaps` — האם יחד
  הם מתירים מעבר מצב שלא תוכנן

**ב. אימות חוקיות השעות מול תלושים אמיתיים.**
`hours.js` הוא 328 שורות של חוקיות עסקית שנלמדה ממערכת קיימת
ומדוחות PDF. הקובץ עומד בפני עצמו ואפשר להדביק אותו. שאלה
טובה: "בהינתן דוח השעות הזה, מה החישוב מחזיר ומה התלוש
אומר?" — פערים כאן עולים כסף אמיתי לכבאים.

**ג. שלב 8 (שכר) — לפני שנכתבת שורת קוד.**
זה תחום ידע, לא תחום קוד: חוקי שעות נוספות בישראל, תוספת
משמרות, כוננות, מילואים, הסכמי שכר של כבאות והצלה. ייעוץ
בשלב התכנון שווה כאן יותר מאשר ביקורת קוד אחר כך.

**ד. ליווי בפעולות הקונסולה.**
התקנת תוסף Trigger Email, הפעלת App Check, הגדרת PITR. אלה
לחיצות שרק בעל הפרויקט יכול לבצע, אבל אפשר להנחות אותו בהן
צעד-צעד, וזה זמן שנחסך.

### לא שווה

**כתיבת קוד לתוך הפרויקט הזה.** 12,000 שורות שאין לגורם
חיצוני גישה אליהן, בלי build ובלי טיפוסים, עם המוסכמות של
עצמן. כל שאלה מחייבת הדבקה ידנית של הקשר, וקוד שנכתב מתוך
הקשר חלקי נראה נכון ושובר דברים שלא נראים במסך. הסיכון
הגדול יותר הוא **שני גורמים שעורכים את אותם קבצים בלי מצב
משותף** — כך נוצרים שינויים סותרים ואבחון שלוקח שעות.

**החלטות ארכיטקטורה שכבר התקבלו.** כל אילוץ בסעיף 2 הוא
תוצאה של החלטה מודעת. הצעה לעבור ל-React או להוסיף bundler
היא הצעה לכתוב את הפרויקט מחדש, לא לשפר אותו.

**פריסה, push, ולחיצות בקונסולה.** אף גורם חיצוני לא יכול
לבצע אותן. רק בעל הפרויקט.

### התפקיד המומלץ

**מבקר ויועץ תחום — לא בונה שני.** תן לו קבצים שעומדים בפני
עצמם (`firestore.rules`, `hours.js`, `rotation.js`,
`readiness.js`, `callout.js`) ובקש ביקורת. את החוקיות העסקית
של שלב השכר — תכנן איתו לפני הכתיבה. את הקוד עצמו — השאר
במקום אחד.

---

## 10. קישורים

- אתר חי — https://elyo102.github.io/ResQ-102/
- קונסולת Firebase — https://console.firebase.google.com/project/station-102
- כללי אבטחה — https://console.firebase.google.com/project/station-102/firestore/rules
- אינדקסים — https://console.firebase.google.com/project/station-102/firestore/indexes
- פונקציות — https://console.firebase.google.com/project/station-102/functions
